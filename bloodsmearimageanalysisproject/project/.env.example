# MongoDB Configuration (Authenticated)
# For local development, use local MongoDB
MONGODB_URI_LOCAL=mongodb://username:password@localhost:27017/bloodsmear?authSource=admin

# For production (Vercel), use MongoDB Atlas
# Note: URL encode special characters in password (@ becomes %40)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bloodsmear?retryWrites=true&w=majority

# Database name
MONGODB_DB_NAME=bloodsmear
